networks:
  sillasol-net:
    external: true

services:
  aria:
    hostname: rokey-gss-backend
    container_name: rokey-gss-backend
    build:
      context: .
    image: steve0314/rokey-gss-backend
    restart: always
    networks:
      - sillasol-net